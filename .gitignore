# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# iOS / Capacitor
ios/App/App/public
ios/App/build
ios/DerivedData
ios/App/Pods
*.xcworkspace/xcuserdata
*.xcodeproj/xcuserdata

# Android / Capacitor
android/app/src/main/assets/public
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
android/app/build
android/build
android/.gradle
android/.idea
android/captures
android/local.properties
android/app/release
android/key.properties
*.keystore
*.jks

# Local App Store screenshot output (remove this line to commit PNGs)
screenshots/app-store/*.png

# NOTE: .env.production is intentionally committed. It holds only the Supabase
# URL and anon key, which are public by design (shipped in the client bundle
# and protected by Row-Level Security). CI needs it to build the live site.
